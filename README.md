# terraform
Terraform resources and examples.

`terraform-main` contains modules for easy steup of AWS environments, and has several example projects.

`serverless-web` contains an example of creating a serverless webapp with terraform.

`hub-spoke-accounts` contains an example of creating a hub-and-spoke network between two AWS accounts with a Transit Gateway, using a shared-services VPC for interface endpoints.
